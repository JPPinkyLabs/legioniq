# Supabase Client Configuration (Required)
# These are PUBLIC keys and safe to expose in the client-side code
VITE_SUPABASE_URL="your-url-here"
VITE_SUPABASE_PUBLISHABLE_KEY="your-key-here"

# Optional: Project ID (not used in code, but useful for reference)
# VITE_SUPABASE_PROJECT_ID="your-project-id-here"

# Note: All sensitive environment variables (OCR_SPACE_API_KEY, OPENAI_API_KEY, etc.)
# are now configured as secrets in Supabase Dashboard for Edge Functions.
# See docs/DEPLOY_GUIDE.md for details.
